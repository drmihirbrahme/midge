<!-- Before filing: please confirm you're running midge, not another tool.
     midge's startup prints LOADING / # mapped experts / READY.
     If you see "waking the giant" or another banner, it's a different build. -->

**What happened**


**Command run**
```
./midge ...
```

**Full output** (up to where it stopped)
```

```

**Environment**
- OS / arch:
- Python version (`python3 --version`):
- Model:
- Backend (C / MLX):
- Output of `./midge check <model-dir>`:
